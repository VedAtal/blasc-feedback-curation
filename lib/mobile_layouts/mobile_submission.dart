import 'package:flutter/material.dart';

class MobileSubmission extends StatefulWidget {
  const MobileSubmission({Key? key}) : super(key: key);

  @override
  _SubmissionState createState() => _SubmissionState();
}

class _SubmissionState extends State<MobileSubmission> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('placeholder'),
    );
  }
}

// MOBILE DEVELOPMENT IN THE FUTURE
