import 'package:flutter/material.dart';

class MobileExamples extends StatefulWidget {
  const MobileExamples({Key? key}) : super(key: key);

  @override
  _ExamplesState createState() => _ExamplesState();
}

class _ExamplesState extends State<MobileExamples> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text(
        'placeholder',
      ),
    );
  }
}

// MOBILE DEVELOPMENT IN THE FUTURE
