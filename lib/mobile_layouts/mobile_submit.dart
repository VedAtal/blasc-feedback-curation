import 'package:flutter/material.dart';

class MobileSubmit extends StatefulWidget {
  const MobileSubmit({Key? key}) : super(key: key);

  @override
  _SubmitState createState() => _SubmitState();
}

class _SubmitState extends State<MobileSubmit> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text(
        'placeholder',
      ),
    );
  }
}

// MOBILE DEVELOPMENT IN THE FUTURE
